#include "main.h"
#include <stdio.h>
/**
 * print_numbers - prints numbers from 1 to 9
 *
 * Return: returns 0
 */

void print_numbers(void)
{
int i;
for(i = "0"; i <= "9"; i++)
	putchar(i);
return (0);

}

